[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["cd eventops && npm ci && npx prisma generate"]

[phases.build]
cmds = ["echo 'Skipping Next.js build for worker service'"]

[start]
cmd = "cd eventops && npm run worker"
