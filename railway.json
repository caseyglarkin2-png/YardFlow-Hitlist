{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "eventops/nixpacks.toml",
    "buildCommand": "cd eventops && rm -rf node_modules/.cache .next && npm ci && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd eventops && npx prisma migrate deploy && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
